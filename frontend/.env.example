# Backend URL for production (set this in Vercel dashboard → Project Settings → Environment Variables)
# In local dev, leave this EMPTY — the Vite dev-server proxy handles /api/* → localhost:5000
VITE_API_URL=https://your-render-service-name.onrender.com
